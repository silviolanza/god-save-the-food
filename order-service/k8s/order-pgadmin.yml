apiVersion: apps/v1
kind: Deployment
metadata:
  name: order-pgadmin-deployment
  namespace: slanza
  labels:
    app: order-pgadmin
spec:
  replicas: 1
  selector:
    matchLabels:
      app: order-pgadmin
  template:
    metadata:
      labels:
        app: order-pgadmin
    spec:
      containers:
        - name: order-pgadmin
          image: dpage/pgadmin4
          ports:
            - containerPort: 5050
          env:
            - name: PGADMIN_DEFAULT_EMAIL
              valueFrom:
                secretKeyRef:
                  name: order-secret
                  key: pgadmin-root-email
            - name: PGADMIN_DEFAULT_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: order-secret
                  key: pgadmin-root-password
            - name: PGADMIN_LISTEN_PORT
              valueFrom:
                configMapKeyRef:
                  name: order-configmap
                  key: pgadmin_listen_port
            - name: SCRIPT_NAME
              valueFrom:
                configMapKeyRef:
                  name: order-configmap
                  key: pgadmin_baseurl
---
apiVersion: v1
kind: Service
metadata:
  name: order-pgadmin-service
  namespace: slanza
spec:
  selector:
    app: order-pgadmin
  ports:
    - protocol: TCP
      port: 5050
      targetPort: 5050
